class CarClass(object):
	def __init__(self, type_, model, name):
		self.type_ = type_
		self.model = model
		self.name = name 
        def get_type(self):
		return 'the type is {} {} {}'.format(self.type_, self.model, self.name)
	
	
	
	
       
